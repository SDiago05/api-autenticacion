# Puerto en el que correra el servidor
PORT=3000

# Clave secreta usada para firmar los tokens JWT (cambiar por una propia y segura)
JWT_SECRET=clave_secreta_super_segura_cambiar_en_produccion

# Tiempo de expiracion del token (ej: 1h, 30m, 7d)
JWT_EXPIRES_IN=1h
